{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to NERDSS","text":"<p>NERDSS is a structure-resolved reaction-diffusion simulation software developed by Johnson Lab, JHU</p>"},{"location":"#quick-start-guide-for-nerdss-setup","title":"Quick Start Guide for NERDSS Setup","text":"<p>Note:</p> <p>If you prefer using Docker for environment setup, jump directly to Step 4.</p> <p>Windows users: It's recommended to use either Windows Subsystem for Linux (WSL) or Docker.</p> <p>Step 1: Clone the Repository</p> <pre><code>git clone https://github.com/mjohn218/NERDSS.git\n</code></pre> <p>Step 2: Install Dependencies</p> <ul> <li>For Ubuntu or WSL:</li> </ul> <pre><code>sudo apt-get update &amp;&amp; sudo apt-get install -y build-essential libgsl-dev\n</code></pre> <ul> <li>For macOS:</li> </ul> <p>Install Homebrew:</p> <pre><code>/bin/bash -c \"$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\"\n</code></pre> <p>Install GCC and GSL:</p> <pre><code>brew update\nbrew install gcc gsl\n</code></pre> <p>Step 3: Compile NERDSS</p> <p>Navigate to the NERDSS directory and compile:</p> <pre><code>cd NERDSS\nmake serial\n</code></pre> <p>The resulting executable will be located in the <code>bin</code> folder.</p> <p>Step 4: Using Docker for Environment Setup (Optional)</p> <p>If you'd rather not install the required tools directly on your machine, you can use Docker:</p> <ul> <li> <p>Install Docker</p> </li> <li> <p>Build the Docker image using the provided Dockerfile:</p> </li> </ul> <pre><code>docker build -t my_nerdss_image .\n</code></pre> <p>Run the Docker container:</p> <pre><code>docker run -it -v $(pwd):/SIMULATION my_nerdss_image\n</code></pre> <p>This command mounts your current directory to the <code>/SIMULATION</code> directory inside the container. Changes made inside the container's <code>/SIMULATION</code> directory will reflect in your host system's current directory and vice versa. You can run the <code>nerdss</code> command in any folder of the container.</p>"},{"location":"#running-a-basic-nerdss-simulation","title":"Running a Basic NERDSS Simulation","text":"<p>Once NERDSS is installed, let's run a basic simulation to understand the philosophy behind NERDSS.</p>"}]}